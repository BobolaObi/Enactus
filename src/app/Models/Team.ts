export class Team{
  image: string
  name: string
  title: string

  constructor(image: string, name: string, title: string) {
    this.image = image;
    this.name = name;
    this.title = title;
  }
}
